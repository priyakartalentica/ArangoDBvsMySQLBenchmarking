package com.arango.pizzaDelivery.service;

import com.arango.pizzaDelivery.entity.Order;
import com.arango.pizzaDelivery.entity.Pizza;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.jupiter.api.Assertions.*;

class OrderServiceTest {
    @Autowired
    OrderService orderService;

    @Test
    public void getOrderService() {
      /*  orderService =new OrderService();
        Order order=new Order();
        order.setOrderId(1L);
        Pizza pizza=new Pizza(1L,"Margarita", (double) 1000,"Veg");
        order.setPizza(pizza);
        order.setMobileNo("7777072243");
        order.setIsDelivered(false);
        order.setDelivaryAddress("Bavdhan, Pune");
        order.setOrderTotalAmount(1000.78);
        System.out.println(order.toString());*/
       // orderService.createOrder(order);


    }
}